import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '02-components',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
